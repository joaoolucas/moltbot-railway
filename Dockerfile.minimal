# Minimal test container
FROM node:22-alpine

WORKDIR /app

RUN echo 'const http = require("http"); http.createServer((req,res)=>{res.end("ok")}).listen(process.env.PORT||8080,()=>console.log("listening on port", process.env.PORT))' > server.js

CMD ["node", "server.js"]
